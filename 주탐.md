주제탐구 연구계획서
 
아래 형식은 참고용 틀이며, 연구에 따라 자유롭게 변형해도 됩니다. (9페이지 이상)

1. 서론
A. 이론적 배경
Embedding
신경망은 이산적인 기호를 직접 처리할 수 없으므로, embedding을 통해 각 token을 고차원 실수 벡터로 mapping한다. embedding 벡터는 학습 초기에는 무작위로 초기화되지만, 경사 하강법에 의해 갱신되며 점차 token 간의 의미적 유사도가 벡터 공간 내 거리로 반영되는 구조로 수렴한다. 이를 통해 이산 기호에 연속적인 기하학적 구조가 부여되며, 신경망이 이를 연산 가능한 표현으로 처리할 수 있게 된다.
Transformer에서는 두 종류의 embedding이 사용된다. token embedding은 어휘 집합 내 각 기호를 고정 차원의 벡터로 투영하고, positional embedding은 sequence 내 각 position에 대한 정보를 부가적인 벡터로 encoding한다. Self-attention 연산 자체는 입력 순서에 대해 등변(permutation equivariance)을 가지므로, position 은 순서 정보를 모델에 명시적으로 주입하기 위한 필수적인 구성 요소이다.
Attention
Attention은 sequence 내 임의의 두 position 간 의존 관계를 단일 연산으로 포착하기 위해 도입되었다. 입력 표현은 선형 변환을 통해 Query(Q), Key(K), Value(V)의 세 행렬로 나타난다. 이 연산을 통해 각 position는 sequence 전체를 참조하여 문맥화된 표현으로 갱신되며, 순환 구조 없이도 장거리 의존 관계를 효과적으로 모델링할 수 있다. 이러한 self-attention을 다중 헤드로 병렬 수행하는 Multi-Head Attention이 Transformer의 핵심 연산 단위를 구성한다.
Seq2Seq
Seq2Seq(sequence-to-sequence)는 가변 길이의 입력 sequence를 받아 가변 길이의 출력 sequence를 생성하는 구조적 틀이다. Transformer 기반 seq2seq는 encoder-decoder 구조로 구성된다. encoder는 입력 sequence 전체를 self-attention을 통해 문맥화된 잠재 표현으로 압축하고, decoder는 encoder 출력에 대한 cross-attention과 이전 출력 token에 대한 masked self-attention을 조합하여 출력 token을 자기회귀적(autoregressive)으로 생성한다. 단일 token을 예측하는 분류 문제와 달리, seq2seq는 출력 sequence 전체에 걸쳐 구조적 일관성이 유지되어야 하므로 학습 난이도가 본질적으로 높다.
Grokking
Grokking은 Power et al.(2022)이 발견한 지연된 일반화(delayed generalization) 현상으로, 신경망이 훈련 데이터에 대해 완전히 과적합한 이후에도 추가적인 최적화를 지속하면 어느 시점에서 검증 정확도가 급격히 상승하는 현상을 가리킨다. 이는 과적합 이후 추가 학습이 일반화 성능을 개선하지 못한다는 고전적 일반화 이론에 대한 반례를 제시한다.
Nanda et al.(2023)은 mechanistic interpretability 관점에서 이 현상을 Memorization, Circuit formation, Cleanup의 세 단계로 분리하였다. 학습 초기에는 훈련 데이터를 직접 암기하는 해로 빠르게 수렴하며, 이후 일반화 회로가 가중치 내에서 점진적으로 형성된다. 최종적으로 암기 성분이 소거되면서 일반화 회로만이 잔존하게 되어 검증 정확도가 급등한다. Liu et al.(2022)은 이를 loss landscape 관점에서 분석하여, 훈련 손실과 테스트 손실의 가중치 norm에 대한 형태 불일치(LU mechanism)가 grokking의 근본 원인임을 제시하였다. 암기 해는 일반화 해에 비해 가중치 norm이 크게 유지되므로, weight decay가 모델을 지속적으로 일반화 해 방향으로 유도하는 핵심 기제로 작용한다.

B. 학술적 가치
신경망의 일반화 메커니즘은 딥러닝 이론의 핵심 미해결 문제 중 하나이다. 이 맥락에서 Power et al.(2022)이 발견한 grokking 현상, 즉 신경망이 훈련 데이터를 완전히 암기한 후 한참 지나서야 갑작스럽게 테스트 정확도가 급등하는 지연된 일반화(delayed generalization)는, 훈련 오차가 충분히 수렴한 이후 추가적인 훈련이 일반화를 오히려 악화시킨다는 고전적 과적합 이론의 예측에 정면으로 반하는 이례적 현상으로, 발표 이후 다수의 후속 연구를 촉발하였다.
후속 연구들은 주로 modular arithmetic을 중심으로 진행되어 왔다. Nanda et al.(2023)은  modular addition에서 훈련된 Transformer가 이산 푸리에 변환(discrete Fourier transform)과 삼각 항등식을 이용하여 덧셈을 원 위의 회전으로 변환하는 알고리즘을 내부적으로 학습함을 reverse engineering으로 밝혔으며, 훈련 과정을 Memorization, Circuit Formation, Cleanup의 세 단계로 구분하였다. Varma et al.(2023)은 이를 일반화 회로와 암기 회로 간의 효율성 경쟁 관점에서 설명하였고, Liu et al.(2023)은 Omnigrok을 통해 grokking이 알고리즘 데이터셋을 넘어 MNIST, 그래프 데이터 등 다양한 영역에서도 발생함을 보였다. Furuta et al.(2024)는 덧셈 이외의 뺄셈, 곱셈, 다항식 등 복합적 modular arithmetic로 분석 범위를 확장하였다.
그러나 기존 연구들은 두 정수 a, b를 입력으로 받아 a ○ b mod p의 결과를 p개의 클래스 중 하나로 예측하는 단일 token 분류 문제에 집중하였으며, 수열 전체를 입력으로 받아 재배열된 수열을 출력하는 seq2seq 형태의 구조적 변환에서 grokking이 발생하는지, 그리고 정렬 기준의 대수적 복잡도에 따라 일반화 양상이 어떻게 달라지는지는 충분히 탐구되지 않았다. 정렬은 원소 간의 순서 관계를 내재함으로써 단순한 입출력 대응을 넘어 수열 전체의 구조를 파악해야 한다는 점에서, 기존 modular arithmetic 연구와 본질적으로 다른 복잡도를 가진다. 또한 m의 배수 우선 정렬이나 최솟값-최댓값 교번 정렬처럼 일부 정렬 기준은 잉여류(Z/mZ) 구조를 정렬 키로 내포하므로, modular arithmetic과 주기적 구조를 공유한다. 본 연구는 이 유사성이 모델 내부 표현에서 실제로 재현되는지를 검증 대상으로 삼는다.
또한 본 연구는 positional embedding과 token embedding이 정렬 알고리즘 학습에서 각각 어떤 역할을 하는지, 그리고 실질적으로 유효한 표현 차원의 수가 몇 개인지를 분석함으로써, Transformer 내부의 표현 학습 메커니즘에 대한 이해를 심화시키는 데 기여할 수 있다.
C. 연구의 독창성
기존 연구는 두 정수의 연산 결과를 단일 token으로 예측하는 분류 문제에 집중하였으나, 본 연구는 길이 N의 수열을 입력으로 받아 재배열된 수열 전체를 출력하는 seq2seq 구조를 채택한다. 이는 grokking 연구에서 출력이 구조화된 sequence인 경우를 본격적으로 다루는 시도에 해당한다.
본 연구에서는 오름차순 정렬처럼 전순서를 따르는 기준부터, m의 배수 우선 정렬처럼 modular arithmetic의 잉여류 구조를 정렬 키로 사용하는 기준, 그리고 최솟값-최댓값 교번 정렬처럼 비선형적 재배열 규칙까지 다양한 대수적 복잡도를 가진 기준들을 비교함으로써, grokking과 구조간의 연관성을 귀납적으로 탐색한다. 또한 positional embedding과 token embedding 중 어느 쪽이 일반화에 더 결정적인지, 표현의 유효 차원 수는 몇 개인지를 함께 분석함으로써, 단순한 현상 확인을 넘어 내부 구조의 embedding 결과 또한 규명하고자 한다. Self-attention은 순열 등변(equivariant)이지만, 정렬 함수 자체는 입력에 대해 순열 불변(invariant)이다. 이 비대칭이 encoder와 decoder에서 positional embedding의 필요성을 다르게 만드는 근거가 된다.

 
 
 2. 연구 문제(Research Question)
연구 문제 1. 정렬 기준의 대수적 구조가 Grokking을 통한 일반화 수렴 속도 및 양상과 어떤 연관성을 가지는가?
기존 grokking 연구는 modular arithmetic이라는 단일 대수 구조에 집중되어 있어, 어떤 구조적 속성이 grokking의 발현 조건 및 수렴 특성을 결정하는지는 아직 충분히 규명되지 않았다. 본 연구는 전순서 기반의 오름차순 정렬, Z/mZ 잉여류 구조를 정렬 키로 내포하는 m의 배수 우선 정렬, 비선형적 재배열 규칙을 따르는 최솟값-최댓값 교번 정렬을 설계하고, 각 기준에 대해 모델 크기·learning rate·weight decay·입력 길이 N·어휘 크기 p·train fraction을 고정한 통제된 조건에서 최소 5개 이상의 시드로 훈련시킨다. 정렬 기준 간 내재적 난이도 차이를 고려하여, raw step 수뿐 아니라 훈련 정확도 포화 시점으로 정규화한 상대적 grokking gap도 함께 보고한다. 단, grokking은 hyperparameter와 weight decay에 민감하므로 발현되지 않을 수 있으며, 이 경우 "어떤 구조에서 grokking이 사라지는가" 역시 유의미한 결과로 해석한다. 이를 통해 정렬 기준의 구조적 특성이 일반화 난이도와 어떤 관계를 갖는지를 귀납적으로 탐색하고, 나아가 seq2seq 구조에서 출력 시퀀스 전체의 일관성이 요구되는 경우에도 기존 단일 토큰 분류에서 관찰된 grokking의 메커니즘이 동일하게 적용되는지를 검토한다.
연구 문제 2. grokking 이후 모델의 내부 표현은 어떤 대수적 구조를 가지는가?
Nanda et al.(2023)은  modular addition에서 grokking이 완료된 모델이 푸리에 기저를 활용한 소수의 주파수 성분만으로 연산을 수행함을 보였다. 이는 일반화 이후 모델의 내부 표현이 구조에 부합하는 저차원 기하학적 구조로 수렴함을 나타낸다. 본 연구는 grokking 완료 모델의 내부 표현에서 PCA 등을 활용하여 유효 차원 수를 측정하고, 해당 차원이 정렬 기준의 대수적 구조로 어떻게 나타나는 지를 분석한다. 
연구 문제 3. seq2seq 정렬에서 positional embedding과 token embedding은 일반화에 대해 각각 어떠한 기여를 갖는가?
정렬은 원소의 값뿐 아니라 출력 시퀀스 내 위치 관계를 동시에 학습해야 하므로, token embedding과 positional embedding이 일반화에 기여하는 방식이 분류와 본질적으로 다를 수 있다. 정렬 함수는 입력 순서에 대해 불변(permutation invariant)이지만 출력은 순서를 가진다는 비대칭성에 주목한다. 이로부터 encoder의 positional embedding은 일반화에 불필요하거나 오히려 해로울 수 있는 반면, decoder의 positional embedding은 필수적이다라는 가설을 세운다. 이를 검증하기 위해 positional embedding을 encoder/decoder로 분리하여 제거하는 ablation을 수행하고, 두 조건의 대비를 통해 가설을 직접 검증한다. 특히 정렬 기준에 따라 두 embedding의 상대적 기여도가 달라지는지를 함께 분석함으로써, 구조적 특성이 표현 학습 메커니즘에 어떻게 반영되는지를 규명한다.

 
3. 선행연구 조사
가. Transformer Model의 기본원리가 되는 연구
Vaswani et al. (2017)은 “Attention Is All You Need”에서 self-attention을 기반으로 한 Transformer architecture를 제안하였다. 이후 grokking 연구들은 주로 small Transformer를 알고리즘에 학습시켜 내부 표현과 일반화된 과정을 분석하므로, 이 연구는 본 연구에서 사용하는 model architecture의 기반이 된다.
나. grokking 현상을 체계적으로 제시한 연구
Power et al. (2022)은 “Grokking: Generalization Beyond Overfitting on Small Algorithmic Datasets”에서 인공 신경망이 작은 algorithmic dataset을 먼저 memorization한 뒤, 긴 optimization 이후 갑자기 일반화를 달성하는 grokking 현상을 보고하였다. 이는 model이 단순 암기를 넘어 데이터의 기저를 학습할 수 있음을 보여주며, 본 연구의 기반이 된다.
Nanda et al. (2023)은 “Progress Measures for Grokking via Mechanistic Interpretability”에서 modular addition을 학습한 small transformer가 Fourier basis와 trigonometric identities를 활용해 generalization하는 내부 알고리즘을 reverse engineering하였다. 이 연구는 grokking을 Memorization, Circuit Formation, Cleanup의 세 단계로 분리하여, 단순한 갑작스러운 성능 변화가 아니라 일반화 회로의 점진적 형성과 암기 성분의 소거가 결합된 과정으로 해석하였다.
다. grokking 분석을 다양한 algorithmic task로 확장한 연구
Chughtai et al. (2023)은 “A Toy Model of Universality: Reverse Engineering How Networks Learn Group Operations”에서 small neural networks가 finite group composition을 수행할 때 representation theory 기반 알고리즘을 학습할 수 있음을 mechanistic interpretability로 분석하였다. 이 연구는 modular addition 중심의 해석을 group-theoretic algorithmic task로 확장하며, 모델들이 유사한 원리를 학습할 수 있지만 세부 circuits와 학습 순서는 달라질 수 있음을 보여준다.
Furuta et al. (2024)은 “Towards Empirical Interpretation of Internal Circuits and Properties in Grokked Transformers on Modular Polynomials”에서 grokking 분석 대상을 modular addition에서 subtraction, multiplication, modular polynomials, multi-task mixture로 확장하였다. 이들은 Fourier Frequency Density(FFD)와 Fourier Coefficient Ratio(FCR)라는 progress measure를 제안하여 operation별 internal representation의 차이를 분석했으며, factorizable polynomials에서는 grokking이 비교적 잘 발생하지만 non-factorizable polynomials에서는 명확한 Fourier pattern이 나타나지 않는 경우가 있음을 보였다. 
Stander et al. (2024)은 “Grokking Group Multiplication with Cosets”에서 permutation group의 group multiplication을 학습한 one-hidden-layer network를 reverse engineering하였다. 이 연구는 group operation을 학습한 인공 신경망의 내부 표현을 coset structure와 연결하여 분석했다는 점에서, group-theoretic task에서의 grokking 해석을 확장한다.
라. Transformer의 sorting task 학습과 내부 구조 분석 연구
Urdshals and Urdshals (2025)은 “Structure Development in List-Sorting Transformers”에서 one-layer attention-only Transformer가 number list sorting을 학습하는 과정에서 내부 attention 구조가 어떻게 발달하는지 분석하였다. 이 연구는 학습이 끝난 model의 attention heads가 vocabulary-splitting과 copy-suppression이라는 두 가지 주요 방식으로 조직됨을 보였으며, 본 연구의 다양한 방법의 정수 정렬에서 model이 단순 memorization을 넘어 어떤 내부 구조를 형성하는지 해석하는 데 참고가 된다.


 
4. 연구내용 및 방법
연구 문제와 관련하여, 연구 내용 및 방법을 구체적으로 작성해 주세요.
공백 포함 4000자 이상(또는 3페이지 이상) 작성해 주세요.
필요시, 소제목을 달아도 됩니다. 그림, 표 규정은 위와 같습니다.

가. 연구 개요 및 실험 패러다임
본 연구는 길이 N의 정수 수열을 입력으로 받아, 정해진 정렬 기준에 따라 재배열된 수열 전체를 출력하는 seq2seq 정렬 과제에서 grokking이 발현되는지, 그리고 정렬 기준의 대수적 구조가 일반화의 수렴 양상과 내부 표현에 어떻게 반영되는지를 실험적으로 규명한다. 기존 grokking 연구가 두 정수 에 대해 a ○ b mod p를 p개 클래스 중 하나로 예측하는 단일 토큰 분류에 한정되었던 것과 달리, 본 연구는 출력이 순서를 갖는 시퀀스 전체인 경우로 패러다임을 확장한다.
연구는 세 단계로 구성된다. 첫째(연구 문제 1), 대수적 복잡도가 상이한 세 가지 정렬 기준에 대해 동일한 조건으로 Transformer를 훈련하여 grokking의 발현 시점과 수렴 속도를 비교한다. 둘째(연구 문제 2), grokking이 완료된 모델의 내부 표현을 추출하여 유효 차원 수와 기하학적 구조를 분석한다. 셋째(연구 문제 3), positional embedding과 token embedding에 대한 ablation을 통해 각 표현 요소가 일반화에 기여하는 방식을 정렬 기준별로 분리 측정한다.
특히 본 연구는 grokking의 발현을 전제하지 않는다. grokking은 암기가 가능한 소규모 데이터, 강한 weight decay, 지연된 일반화라는 특수 조건에서만 관찰되므로, 정렬 과제에서 grokking이 발현되지 않거나 정렬 기준에 따라 발현 여부가 갈리는 것 자체가 유의미한 결과로 간주된다.
나. 정렬 과제 및 데이터셋 설계
(1) 입력·출력의 정의
어휘(값) 집합을 로, 입력을 길이 의 수열 로 정의한다. 출력 는 정렬 기준 함수 에 의해 결정되는 의 재배열이다. 입력 수열은 BOS·EOS·구분자(SEP) 토큰과 함께 토큰화되며, decoder는 출력 수열을 자기회귀적으로 생성한다. 손실은 출력 토큰 위치 전체에 대한 cross-entropy로 정의한다.
(2) 세 가지 정렬 기준
정렬 기준은 대수적 구조의 스펙트럼을 따라 설계한다. 전순서(total order) → 잉여류(주기) 구조 → 비단조(non-monotonic) 재배열의 순으로 구조적 복잡도가 증가하도록 구성한다.
기준 A — 오름차순 정렬(전순서): . 자연수의 전순서만을 이용하는 가장 단순한 기준으로, 학습되어야 하는 핵심 구조는 원소 간 비교(comparison)이다. grokking 발현의 기준선(baseline) 역할을 한다.
기준 B — 잉여류 기반 정렬(주기 구조): 1차 정렬 키를 , 2차 정렬 키를 로 하여 정렬한다(잉여류 인 원소가 먼저 오고, 같은 잉여류 내에서는 오름차순). 이 기준은 의 잉여류 구조를 정렬 키로 내포한다. 본 연구는 이 기준에서 모델 내부 표현이 modular arithmetic 연구(Nanda et al., 2023)에서 관찰된 Fourier형 주기 구조를 재현하는지를 검증 대상으로 삼는다.
기준 C — 최솟값–최댓값 교번 정렬(비단조): 먼저 오름차순으로 정렬한 을 로 교번 재배열한다. 출력의 인접 위치가 단조 관계를 이루지 않으므로, 모델은 값의 순위(rank)와 출력 위치 간의 비선형적 대응을 학습해야 한다.
세 기준은 동일한 입력 분포·동일한 입출력 길이를 공유하므로, 차이는 오직 정렬 규칙의 구조에서 비롯된다.
(3) Grokking 관찰을 위한 데이터
grokking은 (i) 훈련셋에 대한 암기가 가능하면서, (ii) 일반화가 지연되는 환경에서만 관찰된다. 입력 공간이 로 조합적으로 폭발하면 암기가 불가능해져 grokking 대신 일반적 학습이 일어날 수 있으므로, 본 연구는 어휘 크기와 길이를 작게 제한한다(예비 설정: –, –). 전체 입력 집합에서 일정 비율만을 훈련셋으로, 나머지를 검증셋으로 무작위 분할하며, train fraction을 – 범위에서 변화시켜 grokking이 가장 뚜렷하게 나타나는 비율을 탐색한다. train fraction은 grokking 발현 시점에 결정적 영향을 미치므로, 모든 정렬 기준에서 동일한 비율 집합을 사용하여 비교의 공정성을 확보한다.
다. 모델 구조 및 학습 설정
모델은 Vaswani et al.(2017)의 encoder–decoder Transformer를 소규모로 구성한다(예비 설정: layer 2–4, head 4,  128 수준). 최적화는 grokking 유도에 핵심적인 weight decay를 강하게 적용한 AdamW를 사용하며, full-batch 또는 large batch로 최대  step까지 장기 학습한다. weight decay는 암기 해(큰 가중치 norm)에서 일반화 해(작은 norm)로 모델을 유도하는 기제(Liu et al., 2022의 LU mechanism)이므로, 그 강도를 별도로 sweep하여 grokking 발현 조건을 확인한다.
통제변인: 정렬 기준 간 비교의 공정성을 위해 model size, learning rate, weight decay, batch size, 초기화 스케일, 입력 길이 , 어휘 크기 , train fraction을 모든 조건에서 동일하게 고정한다. grokking 발현 시점은 무작위 시드에 따른 분산이 크므로, 각 (정렬 기준 × 설정) 조합에 대해 최소 5개 이상의 시드로 반복 실험하여 분포로 보고한다.
라. [연구 문제 1] 정렬 기준별 grokking 발현·수렴 비교
각 정렬 기준에 대해 훈련 step에 따른 훈련/검증 정확도 곡선을 기록한다. seq2seq 과제이므로 두 가지 정확도를 함께 측정한다: 출력 시퀀스 전체가 정확히 일치하는 시퀀스 단위 exact-match 정확도와, 위치별 토큰이 일치하는 토큰 단위 정확도이다. 전자는 구조적 일관성을, 후자는 부분적 학습 진행을 포착한다.
grokking 발현의 조작적 정의: 훈련 정확도가 포화(예:  이상)된 step 과, 검증 exact-match 정확도가 임계값(예: )을 처음 초과하는 step 을 각각 식별하고, 그 간격 을 grokking gap으로 정의한다. 가 클수록 일반화가 더 지연됨을 의미한다.
교란변인 분리: 정렬 기준은 알고리즘 복잡도 자체가 상이하므로, raw step 수의 단순 비교는 "대수적 구조의 효과"와 "과제 난이도"를 혼동할 위험이 있다. 이를 완화하기 위해 (i) 훈련 정확도가 포화되는 시점 으로 정규화한 상대적 gap, (ii) 동일 train fraction에서의 발현 여부(이진 결과)를 함께 보고하며, 시드 간 분산을 박스플롯으로 제시한다. 이를 통해 "어떤 대수적 구조가 grokking을 더 어렵게/쉽게 만드는가"를 귀납적으로 탐색한다.
마. [연구 문제 2] grokking 이후 내부 표현 분석
grokking이 완료된 모델에서 token embedding 행렬과 잔차 스트림(residual stream) 활성을 추출하여 내부 표현의 구조를 분석한다.
유효 차원 측정: 표현 행렬에 PCA를 적용하여, 누적 분산의 일정 비율(예: , )을 설명하는 데 필요한 주성분 수, 그리고 participation ratio  (여기서 는 고유값)를 계산하여 표현이 얼마나 저차원으로 압축되었는지를 정량화한다. Nanda et al.(2023)이 보였듯, 일반화 이후 모델은 과제 구조에 부합하는 소수의 유효 차원으로 수렴할 것으로 예상된다.
기준별 예상 구조 및 검증:
기준 A(오름차순)에서는 token embedding이 값의 크기에 따라 일렬로 배열되는 거의 1차원적 "수직선(number line)" 구조를 예상한다.
기준 B(잉여류)에서는 token embedding에 이산 푸리에 변환(DFT)을 적용하여, modular arithmetic에서 관찰된 것과 같은 삼각 함수의 중첩이 나타나는지를 검증한다. Furuta et al.(2024)의 Fourier Frequency Density(FFD)를 progress measure로 활용하여, 주기 구조가 표현에 실제로 반영되는지를 정량 비교한다.
기준 C(교번)에서는 값의 순위와 출력 위치 간 비선형 대응이 표현에 어떻게 encoding되는지를 분석한다.
이를 통해 "정렬 기준의 대수적 구조가 내부 표현의 기하로 직접 드러나는가"라는 연구 문제 2에 답한다.
바. [연구 문제 3] Embedding Ablation
정렬은 입력 원소의 값뿐 아니라 출력 시퀀스 내 위치 관계를 동시에 학습해야 하므로, 두 종류 embedding의 역할이 단일 토큰 분류와 본질적으로 다를 수 있다. 정렬 함수는 입력 순서에 대해 불변(invariant)이지만 출력은 순서를 가진다는 비대칭성이 존재한다.
이로부터 다음의 가설을 세운다:
encoder 측 positional embedding은 일반화에 불필요하거나 오히려 해로울 수 있는 반면, decoder 측 positional embedding은 필수적이다.
이를 검증하기 위해 다음 조건들을 비교한다.
(C0) 전체 모델: 학습형 token + positional embedding(기준선).
(C1) positional embedding 전면 제거.
(C2) encoder positional embedding만 제거.
(C3) decoder positional embedding만 제거.
(C4) token embedding을 고정 one-hot(또는 무작위 고정)으로 대체.
각 조건에서 grokking 발현 여부, grokking gap, 최종 exact-match 정확도를 측정한다. C2와 C3의 대비를 통해 위의 비대칭 가설을 직접 검증하며, C4를 통해 학습형 token embedding이 일반화에 필수적인지를 확인한다. 나아가 동일한 ablation을 세 정렬 기준 각각에 적용하여, 각 embedding의 상대적 기여도가 정렬 기준의 구조에 따라 달라지는지를 분석한다. 예컨대 잉여류 기준에서는 주기 구조를 encoding하는 token embedding이, 교번 기준에서는 출력 위치를 encoding하는 positional embedding이 상대적으로 더 결정적일 것으로 예상한다.
사. 분석 및 통계 처리
모든 정량 지표(grokking gap, 유효 차원, FFD, 최종 정확도)는 시드별로 수집하여 평균과 분산을 정리한다. 정렬 기준 간 또는 ablation 조건 간 차이의 유의성은 시드 표본에 대한 비모수 검정(예: Mann–Whitney U) 등으로 검토하며, grokking 곡선은 step 축을 로그 스케일로 시각화하여 발현 시점을 명확히 드러낸다. 내부 표현 분석 결과는 PCA 산점도, embedding의 DFT 스펙트럼, attention 패턴 시각화(Urdshals & Urdshals, 2025의 vocabulary-splitting, copy-suppression 방식)로 제시한다.
아. 예상 결과 및 한계
본 연구는 (i) 정렬 기준의 대수적 복잡도가 증가할수록 grokking gap이 커지거나 발현이 어려워질 것, (ii) 잉여류 기준에서 modular arithmetic과 유사한 Fourier형 저차원 표현이 재현될 것, (iii) decoder positional embedding이 일반화에 결정적인 반면 encoder positional embedding은 그 영향이 작을 것을 잠정 가설로 둔다.
다만 한계로서, grokking은 hyperparameter에 민감하여 의도한 조건에서도 발현되지 않을 수 있으며, 이 경우에도 "어떤 구조에서 grokking이 사라지는가"라는 형태로 결과를 해석한다. 또한 세 정렬 기준의 내재적 난이도 차이를 완전히 제거할 수는 없으므로, 결론은 절대적 수렴 시점이 아니라 통제된 조건 하의 상대적 비교로 한정하여 제시한다.



 
5. 참고문헌
본문과 관련된 참고문헌을 APA 6th 인용 방식으로 작성해 주세요.
(단, 팀원이 직접 읽은 논문에 한해서만 작성해야 합니다.)
(ChatGPT를 통해 얻은 가짜 인용 내용을 복사/붙여넣기 할 경우, 불이익이 있습니다.)
Chughtai, B., Chan, L., & Nanda, N. (2023). A toy model of universality: Reverse engineering how networks learn group operations. In Proceedings of the 40th International Conference on Machine Learning (Proceedings of Machine Learning Research, Vol. 202, pp. 6243–6267). PMLR.
Furuta, H., Minegishi, G., Iwasawa, Y., & Matsuo, Y. (2024). Towards empirical interpretation of internal circuits and properties in grokked Transformers on modular polynomials. Transactions on Machine Learning Research.
Nanda, N., Chan, L., Lieberum, T., Smith, J., & Steinhardt, J. (2023). Progress measures for grokking via mechanistic interpretability. In International Conference on Learning Representations.
Power, A., Burda, Y., Edwards, H., Babuschkin, I., & Misra, V. (2022). Grokking: Generalization beyond overfitting on small algorithmic datasets. arXiv preprint arXiv:2201.02177.
Stander, D., Yu, Q., Fan, H., & Biderman, S. (2024). Grokking group multiplication with cosets. In Proceedings of the 41st International Conference on Machine Learning (Proceedings of Machine Learning Research, Vol. 235, pp. 46441–46467). PMLR.
Urdshals, E., & Urdshals, J. (2025). Structure development in list-sorting Transformers. arXiv preprint arXiv:2501.18666.
Vaswani, A., Shazeer, N., Parmar, N., Uszkoreit, J., Jones, L., Gomez, A. N., ... Polosukhin, I. (2017). Attention is all you need. In Advances in Neural Information Processing Systems (Vol. 30, pp. 5998–6008). Curran Associates, Inc.

